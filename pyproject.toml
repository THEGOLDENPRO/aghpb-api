[project]
name = "aghpb_api"
description = "Rewrite of anime girls holding programming books api."
authors = [
    {name = "Goldy", email = "goldy@devgoldy.xyz"},
]
readme = {file = "README.md", content-type = "text/markdown"}
requires-python = ">=3.8"
license = { file = "LICENSE" }
dependencies = [
    "requests",
    "importlib-metadata; python_version<'3.8'",
    "fastapi[all]"
]

dynamic = ["version"]

[project.urls]
GitHub = "https://github.com/Goldy-Bot/Goldy-Bot-V5"
BugTracker = "https://github.com/Goldy-Bot/Goldy-Bot-V5/issues"

[tool.setuptools.dynamic]
version = { attr = "main.__version__" }